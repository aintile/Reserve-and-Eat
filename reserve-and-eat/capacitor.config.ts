import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Reserve and Eat',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
